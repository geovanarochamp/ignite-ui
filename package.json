{
  "name": "design-system-by-rocketseat",
  "private": true,
  "packageManager":  "pnpm@9.2.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.5"
  }
}
